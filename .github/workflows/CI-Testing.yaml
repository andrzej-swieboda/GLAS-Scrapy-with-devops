name: CI dev pipeline

on: workflow_dispatch   

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    
    - name: Build to test the image
      working-directory: ./Docker

      run: |
        docker build  .
       